# RAPTOR Integration

This directory holds code and notes staged for later integration into the
external RAPTOR pipeline repository.

## Current Contents

- `states/Georgia/`: Georgia RAPTOR category code prepared here before it is
  copied into the RAPTOR repository's `states/Georgia/` path.

This code is not part of the current web app runtime. The web app reads staged
SQLite and GeoPackage outputs directly from `02-Data-Staging/`.
